# -*- mode: snippet -*-
#name : new spec
# expand-env: ((yas/indent-line 'fixed))
# --
`(setq use-underscores nil)``(setq available-exts nil)`define(
	[
${1:$(inject-dependency yas-text)}
	],

	function (${1:$$(setq available-exts yas-text)}) {
		"use strict";
		
		$0
	}
);