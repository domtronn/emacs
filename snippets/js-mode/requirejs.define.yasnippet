# -*- mode: snippet -*-
# name : RequireJs#Define
# key  : define
# --
define(
`(er!? 'quotes)`$0`(if (buffer-file-name) (cadr (split-string (file-name-sans-extension (buffer-file-name)) (repository-root))) (file-name-sans-extension (buffer-name) ))``(er!? 'quotes)`,
  [
    
  ],
  function () {
    'use strict';

    return function () {

    };
  };
);